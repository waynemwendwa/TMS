{
  "name": "tms",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm -w apps/web run dev",
    "dev:api": "npm -w apps/api run dev",
    "build": "npm -ws run build",
    "lint": "npm -ws run lint",
    "format": "prettier -w ."
  },
  "engines": {
    "node": ">=18.18.0"
  }
}
